import { Routes } from '@angular/router';
import { FormuGroupComponent } from './components/formu-group/formu-group.component';

export const routes: Routes = [
    {
        path:'formu-group',
        component:FormuGroupComponent
    }
];
